//spread operator => ...allows an iterable such as an array or string to be
//                 expanded into separate elements (unpack the element)


let numbers = [1,2,3,4,5]

let maximum = Math.max(...numbers)
let minimum = Math.min(numbers)

console.log(maximum)  //unpack from the array and see them individually
console.log(minimum)   // sees as single array






