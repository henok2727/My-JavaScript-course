//Math => built in object that provides a collection of properties and methods

/*
const x = Math.PI;
const y = Math.E;

console.log(x);
console.log(y);  */

let a = 3.95;
let z;

z = Math.round(a)  // rounds to the close natural number
/*z = Math.floor(a)  // always round down
z = Math.cell(a)    //always roundn up  -opposite of floor
z = Math.trunc(a)   //eliminate decimal points
z = Math.pow(a, b)  //power - exponent
z = Math.sqrt(x)   //square root
z = Math.log(x)
z = Math.sin(x)  // x is radian
z = Math.cos(x)
z = Math.tan(x)
z = Math.abs(x)  //absolute value   - positive value
z = Math.sign(x)  // 1 or 0 oe -1

console.log(z);
*/

let max = Math.max(x, y , z); //mzx of those three
let min = Math.min(x, y, z);

console.log(max);
